# Pions ドメイン用語集

Pionsの永続的なエージェント委譲におけるオペレーション、ワーカー、結果、および表示に関する用語を定義する。

## Operation（オペレーション）

1回のワーカー実行を要求する、永続的で一意に識別されるリクエスト。作成、開始配送、結果受理、停止、キャンセル、状態不明、および表示終了処理の証跡と、結果、実効設定、表示上の識別情報を所有する。別のオペレーションとの親子関係、改訂、または再試行の系列は所有しない。

## Worker（ワーカー）

1つのオペレーションに割り当てられた実行主体。プロセスや目に見えるターミナル状態は稼働中であることの証跡であり、意味上の完了を証明しない。

## Worker identity（ワーカー識別情報）

ワーカーのプロセス、プロセス実体と開始、Piセッション、および所有ワークスペースの根ペインを対象オペレーションへ結び付ける識別情報。秘密情報を含まず、ワーカーの稼働、停止、または結果の正しさを単独では証明しない。

## Worker protocol（ワーカープロトコル）

ワーカーが識別情報、開始、および結果をランタイムへ伝え、ランタイムが受理を確認するための認証済みの対話。開始指示の受理と結果受理は異なる事実であり、それぞれの確認応答は対応する受理後にのみ成立する。

## Result（結果）

1つのワーカーから受理された、永続的な意味上の出力。ワーカーの最終回答であるUTF-8テキスト1件だけを、不正な文字列を含まない正確なバイト列としてオペレーションが直接所有し、Unicode正規化や改行変換を行わない。オペレーションはそのバイト数、SHA-256ダイジェスト、および結果受理識別子を結果とともに永続化し、再取得時には完全性を検証した同じバイト列だけを出力として返す。汎用の成果物識別子や成果物ストアを介さない。

## Worker execution evidence（ワーカー実行証拠）

ワーカー実行で確定した使用量と、各ツール呼び出しの識別子、ツール名、および成否を記録した証跡。エージェントが失敗した場合は、モデルプロバイダーが返したエラー文を解釈せず、UTF-8で4096バイトを上限にそのまま含む。エラー文の意味の判断は呼び出し元に委ねる。プロンプト、モデル応答履歴、ツールの入力または出力の本文を含まず、結果の正しさ、安全性、または採用を意味しない。

## Result acceptance（結果受理）

結果本文の完全性検証、既存上限とUTF-8としての妥当性の検査、および正確なバイト列、バイト数、SHA-256ダイジェスト、結果受理識別子の永続化を終え、変更不能な記録として一体で確定・公開された時点で成立する、オペレーションごとに最大1つの事実。必要な永続化が完了するまで受理を公開せず、中断時に部分結果を返さない。途中状態や確認応答の成否とは区別する。同じ受理要求や同じ内容の再送は同じ事実に合流し、異なる内容では置き換えない。再送でも認証と対象権限を改めて検査する。受理済みの結果は自動削除せず、利用者がPionsの保存領域を明示的に削除するまで保持する。受理は結果の正しさや採用を意味しない。

## Result retrieval（結果再取得）

結果受理が成立したオペレーションについて、保存済みのバイト数とSHA-256ダイジェストに対して実際のバイト列を検証し、UTF-8として安全な断片と不透明なカーソルで同じ結果を返す、副作用のない読み取り。結果受理を待機または成立させず、未受理、破損、および検査不能を区別する。元のハンドルやPiセッションを失った後も同じ結果を再取得できる。取得時には現在も有効な取得権限を検査し、オペレーション識別子を知っていることだけを権限としない。

## Operation inspection（オペレーション状態取得）

オペレーション識別子から、Runtimeが永続化した版付き状態、開始配送、結果受理、停止確認、状態不明の理由、および表示終了処理診断を副作用なく取得する読み取り。結果本文を含めず、作業開始、実行再開、または結果受理の権限を与えない。現在の取得権限を検査し、別の信頼済みリポジトリにおける存在や保存状態を識別できる詳細を返さない。

## Result acceptance identifier（結果受理識別子）

公開済みの結果受理を一意に指す変更不能な識別子。オペレーション、受理要求、および受理した正確なバイト列から導出し、受理要求識別子とは異なる。再送時の確認応答と公開済み証跡の参照に使用する。

## Runtime（ランタイム）

オペレーションを開始し、結果を返し、親からのキャンセル時にワーカーの停止を要求する、呼び出し元向けのモジュール。利用終了時には新規の開始を拒否し、受け付け済みの作成と復旧、実行中のワーカーを待つ。呼び出し元がバックエンド、永続化、表示の詳細を直接調整することはない。

## Self-settlement（自己確定）

ワーカーが成功または失敗の実行証跡を確定した時点。結果受理やワーカー停止が未確定の場合は終端完了とは異なる。

## Terminal completion（終端完了）

結果受理とワーカー停止確認が永続化された後に到達する、オペレーションの変更不能な成功状態。

## Presentation（表示）

人間または外部の観測者に向けたオペレーション状態の投影。稼働状況や注意の必要性を報告できるが、意味上のオペレーション状態を作成したり上書きしたりできない。

## Worker workspace（ワーカーワークスペース）

1つのオペレーション専用にPionsが作成し、作成直後に所有記録として永続化するHerdrワークスペース。委譲元のワークスペースやペインを分割せず、フォーカスを移さずに作成し、ワーカーはその根ペインで動く。表示名は`Pions <短いOperation識別子>`であり、作業内容を含めない。Pionsが作成・所有していないワークスペースは同一性の照合対象にも終了処理の対象にもならない。

## Dispatcher（配送主体）

開始指示をワーカーへ配送するランタイム実体。配送世代ごとに1つの開始配送権限を持ち、引き継ぎでは旧配送主体の失効確認と後継配送主体への権限移転を区別する。

## Start acceptance state（開始受理状態）

ワーカーが永続記録を検査して返す、未受理、受理済み、または不明の閉じた状態。未受理だけが再検査後の配送を許し、不明を未受理へ変換しない。

## Start delivery authority（開始配送権限）

特定の配送主体が、1つのオペレーションの現在の配送世代で開始指示を送信できる権限。取得と送信処理への進入は別々の永続的な事実であり、キャンセルとの順序判定に使用する。引き継ぎでは旧配送主体の権限失効と、ワーカーによる旧世代拒否の確認を必要とする。

## Delivery generation（配送世代）

開始指示を送信できる配送主体の引き継ぎ順序を表す、単調増加する世代。世代番号の保存だけでは旧配送主体の遮断を証明せず、ワーカーが世代更新と開始受理を直列化して返す確認応答によって確定する。

## Start instruction acceptance（開始指示受理）

ワーカーが開始指示を永続化し、プロンプトを最大1回だけ投入すると確定した事実。同一内容の再送では同じ受理事実を返し、異なる内容、旧配送世代、期限切れ、または受理状態不明を新しい受理へ変換しない。受理と、その確認応答を配送主体が観測した事実は区別する。

## Operation completion response（オペレーション完了応答）

呼び出し元が成功したオペレーションの待機から受け取る直接応答。意味上の出力である結果、表示終了処理証跡、および終了処理診断を別々の項目として束ねる。終端完了そのものや版付きの永続スナップショットではなく、診断保存に失敗した場合はその場でしか返せない処理不能も含める。

## Presentation cleanup（表示終了処理）

結果受理とワーカー停止確認を終えた成功オペレーション、またはワーカー停止を確認できたキャンセル済みオペレーションについて、Pionsが所有ワークスペースの同一性を照合して閉じる処理。処理識別子と対象ワークスペース識別情報を結び付けた未完了記録を副作用より先に保存し、完了、または未確認を永続化する。復旧時にも現在のワークスペース一覧を再照合し、未完了記録だけを根拠に閉じる操作を繰り返さない。失敗または状態不明のオペレーションは対象外であり、ワークスペースを調査用に残す。完了は関連する実行主体や外部資源の安全性を証明しない。

## Cleanup diagnostic（終了処理診断）

ワーカーや所有ワークスペースの終了処理、またはその証跡保存に残る未解決の問題。受理済みの結果とは独立し、利用者が終了処理の不確実性を把握するための情報となる。詳細を永続化できた診断は版付きスナップショットから再取得し、診断自体を保存できなかった事実は揮発情報を永続記録へ混ぜず、その処理を待つ直接の完了応答で通知する。
