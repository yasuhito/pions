# Pions ドメイン用語集

Pionsの永続的なエージェント委譲におけるオペレーション、ワーカー、結果、および表示に関する用語を定義する。

## Operation（オペレーション）

1回のワーカー実行を要求する、永続的で一意に識別されるリクエスト。自身のライフサイクルの証跡、結果、系譜、実効ポリシー、表示上の識別情報を所有する。

## Worker（ワーカー）

1つのオペレーションに割り当てられた実行主体。プロセスや目に見えるターミナル状態は稼働中であることの証跡であり、意味上の完了を証明しない。

## Worker identity（ワーカー識別情報）

ワーカーのプロセス、プロセス実体と開始、Piセッション、および所有ワークスペースの根ペインを対象オペレーションへ結び付ける識別情報。秘密情報を含まず、ワーカーの稼働、停止、結果の正しさ、または資源への権限を単独では証明しない。

## Worker profile intended use（ワーカープロファイル用途）

ワーカープロファイルが汎用、読み取り、正式レビュー、書き込み、または改訂・再試行のどの用途を担う候補かを閉じて宣言する設定。汎用は`pions_delegate`が起動する汎用サブエージェントの用途であり、Pi組み込みツールの範囲で読み書きとコマンド実行を行い、外部着手許可や資源証明を要求しない。用途ごとの必須保証を弱めるフォールバックには使わず、必要条件が不足するプロファイルはオペレーション作成前に拒否する。候補用途の宣言と検査合格は、本番での登録、有効化、または段階導入の承認を意味しない。

## Worker protocol（ワーカープロトコル）

ワーカーが識別情報、開始、および結果をランタイムへ伝え、ランタイムが受理を確認するための認証済みの対話。開始指示の受理と結果受理は異なる事実であり、それぞれの確認応答は対応する受理後にのみ成立する。

## Result（結果）

1つのワーカーから受理された、永続的な意味上の出力。ワーカーの最終回答であるUTF-8テキスト1件だけを、不正な文字列を含まない正確なバイト列としてオペレーションが直接所有し、Unicode正規化や改行変換を行わない。オペレーションはそのバイト数、SHA-256ダイジェスト、および結果受理識別子を結果とともに永続化し、再取得時には完全性を検証した同じバイト列だけを出力として返す。汎用の成果物識別子や成果物ストアを介さない。オペレーションが版付きの結果形式を要求する場合は、その識別子、版、および検証主体を作成時に固定し、同じ形式の検証規則を後から差し替えない。

## Result format（結果形式）

正式レビューの結果本文へ適用する、形式識別子、版、正規化規則識別子、信頼済み期待値、および結果形式検証主体の変更不能な組。結果形式検証主体の識別情報は、検証主体識別子、版、および実際の検証規則と実装を表すダイジェストから成り、同じ識別子と版へ異なるダイジェストを再登録しない。

## Result format rejection（結果形式拒否）

実際の結果バイト列がオペレーションへ固定された結果形式、検証主体識別情報、または信頼済み期待値へ適合しなかったため、結果受理を成立させなかった型付きの事実。ワーカーやプロトコルの失敗、および結果受理後の外部利用先による採用拒否とは区別する。

## Worker execution evidence（ワーカー実行証拠）

ワーカー実行で確定した使用量と、各ツール呼び出しの識別子、ツール名、および成否を記録した証跡。プロンプト、モデル応答履歴、ツールの入力または出力の本文を含まず、結果の正しさ、安全性、または採用を意味しない。

## Result acceptance（結果受理）

結果本文の完全性検証、既存上限とUTF-8としての妥当性の検査、要求された版付き結果形式と信頼済みの期待値への適合検査、および正確なバイト列、バイト数、SHA-256ダイジェスト、結果受理識別子の永続化を終え、変更不能な記録として一体で確定・公開された時点で成立する、オペレーションごとに最大1つの事実。必要な永続化が完了するまで受理を公開せず、中断時に部分結果を返さない。途中状態や確認応答の成否とは区別する。同じ受理要求や同じ内容の再送は同じ事実に合流し、異なる内容では置き換えない。再送でも認証と対象権限を改めて検査する。受理済みの結果は自動削除せず、利用者がPionsの保存領域を明示的に削除するまで保持する。形式への適合は、結果の正しさや採用を意味しない。

## Result retrieval（結果再取得）

結果受理が成立したオペレーションについて、保存済みのバイト数とSHA-256ダイジェストに対して実際のバイト列を検証し、UTF-8として安全な断片と不透明なカーソルで同じ結果を返す、副作用のない読み取り。結果受理を待機または成立させず、未受理、破損、および検査不能を区別する。元のハンドルやPiセッションを失った後も同じ結果を再取得できる。取得時には現在も有効な取得権限を検査し、オペレーション識別子を知っていることだけを権限としない。

## Operation inspection（オペレーション状態取得）

オペレーション識別子から、Runtimeが永続化した版付き状態、起動証跡、開始配送、結果受理、停止確認、および終了処理診断を副作用なく取得する読み取り。結果本文を含めず、着手許可、実行再開、または結果受理の権限を与えない。現在の取得権限を検査し、別の信頼済みリポジトリにおける存在や保存状態を識別できる詳細を返さない。

## Result acceptance identifier（結果受理識別子）

公開済みの結果受理を一意に指す変更不能な識別子。オペレーション、受理要求、および受理した正確なバイト列から導出し、受理要求識別子とは異なる。再送時の確認応答と公開済み証跡の参照に使用する。

## Runtime（ランタイム）

オペレーションを開始し、その結果を返し、オペレーションのサブツリーをキャンセルする、呼び出し元向けのモジュール。利用終了時には、終了開始後の新規オペレーション開始と改訂予約を拒否し、終了開始前に開始を受け付けた実行中のオペレーションがすべて確定するのを待ってから、所有する永続資源を閉じて排他的な所有権を解放する。呼び出し元がバックエンド、永続化、子チャネル、表示の詳細を直接調整することはない。

## Self-settlement（自己確定）

ワーカーが自身の成功または失敗という結果を生成した時点。子孫や結果の引き渡しが未確定の場合は終端完了とは異なる。

## Terminal completion（終端完了）

自身の結果が永続化され、必要なすべての子孫と結果の引き渡しが確定した後に到達する、オペレーションの変更不能な終端。

## Presentation（表示）

人間または外部の観測者に向けたオペレーション状態の投影。稼働状況や注意の必要性を報告できるが、意味上のオペレーション状態を作成したり上書きしたりできない。

## Worker workspace（ワーカーワークスペース）

1つのオペレーション専用にPionsが作成し、作成直後に所有記録として永続化するHerdrワークスペース。委譲元のワークスペースやペインを分割せず、フォーカスを移さずに作成し、ワーカーはその根ペインで動く。表示名は`Pions <短いOperation識別子>`であり、作業内容を含めない。Pionsが作成・所有していないワークスペースは同一性の照合対象にも終了処理の対象にもならない。

## Coordinator（コーディネーター）

オペレーションの着手可否、改訂結果採用、外部ワークスペースの存続、および外部資源の後始末を判断する外部主体。ワーカーと所有ワークスペースの終了処理はランタイムの責任と区別する。

## Startup receipt（起動証跡）

外部へ許可を求める時点で永続化された、対象オペレーション、ワーカー実体、実効・観測設定、ワークスペース、権限、レビュー対象識別子、および期限を結び付ける変更不能な証跡。認証用の秘密情報を含まず、実行中の対象不変性を意味しない。

## External review allocation（外部レビュー割り当て）

信頼済み外部主体が発行し、1つの正式レビューを不透明なレビュー対象識別子、ワーカープロファイル、期限、利用上限、および外部識別情報へ固定する変更不能な宣言。Pionsは発行せず、発行元の認証と固定値の照合だけを行い、対象の内容、引き渡し、対象版、レビュー軸、外部実行識別情報の意味や現在状態を判断しない。

## External review allocation reservation（外部レビュー割り当て予約）

認証・照合済みの外部レビュー割り当てと束縛要求を、作成予定の1つのオペレーション識別子へ占有させる未公開の永続状態。同じ要求識別子と同じ内容の再送は合流するが、異なる内容や別オペレーションへの再利用は拒否する。中断、状態不明、または期限経過だけを理由に未使用へ戻さない。

## External review allocation binding（外部レビュー割り当て束縛）

外部レビュー割り当て予約に固定されたオペレーションが、そのレビュー対象識別子、プロファイル、および外部識別情報を変更不能なオペレーション事実として所有すること。束縛は着手許可、実行再開、外部側の現在権限やワークフロー状態、または結果採用を意味しない。

## Start authorization（着手許可）

特定のオペレーションと起動証跡に対して、権限を持つコーディネーターが作業開始を認めたという永続的な判断。許可済みは開始済みではなく、後の停止要求は許可の上書きではない。

## Start gate（着手ゲート）

外部判断と起動証跡の再照合を必要とする、ワーカーの作業開始境界。許可の要否と判断の状態は、オペレーションの実行進捗とは別の情報である。

## Dispatcher（配送主体）

開始指示をワーカーへ配送するランタイム実体。配送世代ごとに1つの開始配送権限を持ち、引き継ぎでは旧配送主体の失効確認と後継配送主体への権限移転を区別する。

## Start acceptance state（開始受理状態）

ワーカーが永続記録を検査して返す、未受理、受理済み、または不明の閉じた状態。未受理だけが再検査後の配送を許し、不明を未受理へ変換しない。

## Start delivery authority（開始配送権限）

特定の配送主体が、1つのオペレーションの現在の配送世代で開始指示を送信できる権限。取得と送信処理への進入は別々の永続的な事実であり、キャンセルとの順序判定に使用する。引き継ぎでは旧配送主体の権限失効と、ワーカーによる旧世代拒否の確認を必要とする。

## Delivery generation（配送世代）

開始指示を送信できる配送主体の引き継ぎ順序を表す、単調増加する世代。世代番号の保存だけでは旧配送主体の遮断を証明せず、ワーカーが世代更新と開始受理を直列化して返す確認応答によって確定する。

## Start instruction acceptance（開始指示受理）

ワーカーが開始指示を永続化し、プロンプトを最大1回だけ投入すると確定した事実。同一内容の再送では同じ受理事実を返し、異なる内容、旧配送世代、期限切れ、または受理状態不明を新しい受理へ変換しない。受理と、その確認応答を配送主体が観測した事実は区別する。

## Execution workspace（実行ワークスペース）

識別情報、場所、起点となる版、所有主体または所有主体が不明であること、および利用権限を持つ作業場所。利用権限と削除権限は異なり、登録証明の発行元認証は内容の正しさまで保証しない。

## Workspace proof（ワークスペース証明）

信頼済み主体が、実行ワークスペースの識別情報、場所、起点となる版、所有主体または所有主体が不明であること、および対象オペレーションとの関係を示す変更不能な証跡。発行元の認証は内容の正しさや実行中の不変性を保証せず、権限目録との関係や必要な観測・強制は資源証明で別に示す。

## Writer workspace（書き込みワークスペース）

正規の起動主体が作成して所有し、書き込み権限を宣言した実行ワークスペース。書き込み範囲の宣言と照合は、範囲外書き込みの阻止や基本ソフトウェア・コンテナによる隔離を意味しない。

## Review subject（レビュー対象）

正式なレビューが評価する外部対象を指す不透明な識別子。オペレーション作成時に固定するが、Pionsは対象のファイル、バイト列、添付、依存関係、形式、または保持を受理・複製・管理しない。

## Revision（改訂）

受理済みの結果に対する差戻しを、新しいオペレーションとして表す、回数上限を持つ線形の関係。対象の実際の結果、系列起点、改訂番号、理由、依頼主体に結び付き、元のオペレーションと結果を変更しない。

## Revision series（改訂系列）

最初の改訂対象を起点として、改訂、再試行、および改訂結果採用を線形に結び付ける台帳。系列識別子、系列全体の回数上限、単調増加する試行番号、および改訂番号を所有し、再試行によって上限や番号を初期化しない。台帳への追記は起点オペレーションの状態や結果を変更しない。

## Revision reservation（改訂予約）

新しい改訂または再試行のオペレーションを作る前に、依頼識別子、対象結果または失敗オペレーション、系列、番号、上限、理由、依頼主体、実行タスク、および必要な再試行安全証明を固定する記録。同じ依頼の再送は同じ予約へ合流し、異なる内容では置き換えない。予約後のオペレーション作成が中断した場合は、保存済みの実行タスクとオペレーション識別子から再開する。

## Retry（再試行）

結果受理前に失敗したオペレーションを変更せず、別のオペレーションとして処理を再度要求する関係。上限は最初の失敗まで遡る系列全体に属し、再試行の再試行でも初期化されない。

## Revision result adoption（改訂結果採用）

改訂番号に対して、どの正当な再試行結果を系列へ接続するかを、結果採用権限を持つ主体が決める変更不能な判断。番号ごとの採用は最大1つであり、再試行の成功だけでは成立しない。

## Permission manifest（権限目録）

ワーカーに許可するツール、読み取り、書き込み、コマンド、ネットワーク、および外部資源を閉じて宣言するもの。未指定の権限を許可とは解釈せず、要求権限と実効権限は意味上の完全一致を必要とする。権限の宣言、現在の観測、および実行中の強制は独立しており、必要な観測・強制保証は資源証明で別に示す。

## Resource proof policy（資源証明方針）

オペレーションに資源証明を要求するかを作成時に固定する方針。証明不要または証明必須を明示し、未指定を証明不要へ変換しない。証明不要は安全性や副作用不在の保証ではない。外部着手許可の方針とは独立して扱い、書き込みや正式レビューに必要な証明を方針の組み合わせや証明不要の読み取り経路への切り替えで回避できない。

## Resource proof（資源証明）

外部の正規ランチャーまたは信頼済み登録主体が発行する、実行ワークスペース、権限目録、資源、競合範囲、利用範囲と利用方式、実行主体、対象オペレーション、資源取得処理、開始試行、有効期間または世代を結び付けた証跡。各権限制約について宣言一致、現在の観測、および実行中の強制に必要な根拠を含む。資源の正規形には名前空間と正規化規約の版を結び付け、登録更新後も保存済み要求を再解釈しない。競合範囲は別名や範囲の重なりを含み、名前空間の違いや要求の不一致だけを非競合の証拠としない。発行元の認証と内容の正しさは別の検証対象であり、保存済みであっても現在の有効性を保証しない。

## Resource validation（資源再検査）

保存済みの資源証明を変更せず、そのダイジェスト、資源、対象オペレーション、要求された利用方式について、現在の競合防止、引き渡し、期限、世代、および失効状態を改めて検査すること。結果は有効、無効、検査不能を区別し、検査主体、根拠、対象、有効期間または世代、および今回の開始試行への結び付きを持つ。有効という結果は同じ資源取得処理から開始への利用継続と引き渡しまで競合を防ぐ場合にだけ開始根拠となり、別の取得や古い結果を通行証として再利用しない。検査不能はオペレーションの終端状態である状態不明とは異なる。

## Resource acquisition（資源取得処理）

1回の開始試行に必要な競合資源をまとめて後続利用へ引き渡す、永続的に識別される処理。資源の競合保護と実行主体のアクセス許可を別の状態として扱い、実行主体へアクセスを許可した場合はその遮断後にだけ解放できる。解放済みは資源を保持していないことが確認済みである事実を表し、未解決やオペレーションの終端から推論しない。

## Resource cleanup policy（資源後始末方針）

正常終了後の資源解放を、事前委任された範囲で自動実行するか、競合保護を維持してコーディネーターの明示判断を待つかを、オペレーション作成時に固定する方針。判断待ち、コーディネーター不在、または保持期限切れを理由に自動解放へ切り替えず、安全上必要な停止とアクセス遮断は判断待ちにしない。

## Resource cleanup operation（資源後始末処理）

既存の資源取得処理について、状態照会、実行主体のアクセス遮断、および資源解放を復旧する、取得とは別に永続的に識別される処理。新しい取得を作る処理ではなく、有限の実行予算と認証済み依頼主体に結び付く。

## Resource cleanup authority（資源後始末権限）

対象資源について許された状態照会、アクセス遮断、または解放を明示的に要求する権限。読み取り、着手許可、結果採用、新規取得、および作業再開の権限とは独立し、自動安全処理も事前に委任された対象と操作だけを実行できる。

## Authority registration（主体登録）

資源証明の発行元と、資源の取得・現在状態の検査・アクセス遮断・解放を担う資源アダプターを、版または世代付きで結び付ける特権設定。発行元の認証・信頼状態と、資源アダプターの権限・応答は別々の契約として検査し、同じ実装主体が両方を担っても意味を混同しない。オペレーション作成時の登録内容は変更せず、開始前には現在の失効状態を別に検査する。現在の登録による権限拡大、同じ主体識別子への別主体または別方式の差し替え、および失効時の自動的な主体変更を認めない。

## Resource adapter identity（資源アダプター識別情報）

資源アダプターの識別子、版、用途、および実装のダイジェストから成る識別情報。主体登録の一部として固定し、信頼済みブートストラップの承認一覧と照合する。ダイジェスト対象の実装と実行関数が対応することは登録主体の責任であり、パス一致、ダイジェストの一致、または非本番試験の合格だけから本番利用の承認を推論しない。

## Trusted bootstrap（信頼済みブートストラップ）

利用者が所有し、正式レビュー統合を構成する外部の起動コード。Pionsの公開モジュール契約版、利用先リポジトリの正規識別情報とその検証主体、承認済み資源アダプター識別情報、および導入区分を固定し、未信頼リポジトリの通常設定から資格情報、権限、アダプター、または有効化範囲を取得しない。

## Formal review integration module identity（正式レビュー統合モジュール識別情報）

正式レビュー統合の公開入口とその契約版を指す変更不能な識別情報。パッケージの配置場所や利用先リポジトリの内容とは独立し、信頼済みブートストラップが期待版との一致を検査する。

## Staged rollout decision record（段階導入判定記録）

候補となるワーカープロファイル用途ごとに、検証済みの保証、試験結果、残る未達条件、および本番有効化の承認状態を記録する証跡。個別実装Issueの完了や設定検査の合格だけから本番有効化を推論しない。

## Retry clearance（再試行安全証明）

状態不明となった処理について、旧ワーカーと関連する実行主体の停止または実効的なアクセス遮断、および影響資源の競合不在と適切な引き渡しを示す証跡。安全確認は系列ではなく影響資源に結び付き、同じ資源を使う独立した新規オペレーションでも省略できない。

## Operation completion response（オペレーション完了応答）

呼び出し元が成功したオペレーションの待機から受け取る直接応答。意味上の出力である結果、表示終了処理証跡、および終了処理診断を別々の項目として束ねる。終端完了そのものや版付きの永続スナップショットではなく、診断保存に失敗した場合はその場でしか返せない処理不能も含める。

## Presentation cleanup（表示終了処理）

結果受理とワーカー停止確認を終えた成功オペレーション、またはワーカー停止を確認できたキャンセル済みオペレーションについて、Pionsが所有ワークスペースの同一性を照合して閉じる処理。処理識別子と対象ワークスペース識別情報を結び付けた未完了記録を副作用より先に保存し、完了、または未確認を永続化する。復旧時にも現在のワークスペース一覧を再照合し、未完了記録だけを根拠に閉じる操作を繰り返さない。失敗または状態不明のオペレーションは対象外であり、ワークスペースを調査用に残す。完了は関連する実行主体や外部資源の安全性を証明しない。

## Cleanup diagnostic（終了処理診断）

ワーカーや所有ワークスペースの終了処理、またはその証跡保存に残る未解決の問題。受理済みの結果とは独立し、コーディネーターが終了処理の不確実性を把握するための情報となる。詳細を永続化できた診断は版付きスナップショットから再取得し、診断自体を保存できなかった事実は揮発情報を永続記録へ混ぜず、その処理を待つ直接の完了応答で通知する。
