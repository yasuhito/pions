# Pi CLI可視ワーカーの実現可能性プロトタイプ

Issue #24の設計判断に使った、使い捨てプロトタイプの一次資料である。結論と実行時の観測は`REPORT.md`、主要な実験コードは`worker-extension.ts`と`host.mjs`を参照する。

実行時の設定には一時的な権限、プロセス識別子、ソケットパスが含まれるため、このブランチには保存していない。`REPORT.md`に記録された一時パスは実験時点の値であり、再実行手順ではない。
