# Issue #23 手動スモークテスト

## 位置付け

この試験は、未変更の`code-review`スキルを実モデルへ与えたときに、モデルが2つの独立したレビューを`pions_delegate`へ委譲することを確認する受け入れ証拠である。モデルによるツール選択は確率的であり、この結果は将来の実行を形式的に保証しない。この試験コマンドは通常の自動テストスイートへ含めない。

## 実行条件

- 実行日: 2026-09-07
- 固定点: `origin/main`（実行時の解決値: `af905f879b4cd0ca451db6aaf3b118216ba5e69a`）
- 委譲元およびレビューワーカーのモデル: `openai-codex/gpt-5.6-sol`
- 思考レベル: `medium`
- Piのモデルレジストリー確認: `pi --list-models`に`openai-codex  gpt-5.6-sol`が利用可能かつ認証済みとして掲載された
- `code-review`スキル: `/home/yasuhito/.agents/skills/code-review/SKILL.md`
- 実行前後のスキルSHA-256: `47f4e52c21694def9c7c11cbfbf891ca35eac7a93e395797515be3c8a409ae50`
- プロジェクト設定によるモデル上書き: なし

実行コマンドは次のとおり。

```bash
pi --approve \
  --provider openai-codex \
  --model gpt-5.6-sol \
  --thinking medium \
  --mode json \
  -p '/skill:code-review origin/main'
```

## 観測結果

PiのJSONイベント列では、同じアシスタントメッセージから次の2つの`tool_execution_start`が連続して発生し、両ツールが完了する前に双方の開始が記録された。これはPiの並列ツール実行として構成されたことを示す。

| 軸 | Operation | Pions所有のHerdrペイン | バイト数 | ダイジェスト | 切り詰め |
| --- | --- | --- | ---: | --- | --- |
| Standards | `5f7a7899-5b77-4ca3-9771-1ef4e2853bbe` | `wS9:p13` | 1866 | `sha256:3c92d2831a38d93dda1bf2589533bcaaef0898779e96f4826c4787531cda3a7a` | なし |
| Spec | `023cc202-9f0d-41b0-855c-9ee6f7062cb6` | `wS9:p12` | 2190 | `sha256:a4e8436230e8487c67ba956af1b7e0b11b5135649d1c32c47e2ea6b8fa3d0c36` | なし |

2回のツール実行はいずれも成功し、それぞれのレビュー本文と診断詳細が親エージェントへ返った。親エージェントの最終応答には`## Standards`と`## Spec`が別々に含まれ、既存スキルの形式で集約された。完全な結果は各`Operation`の永続記録に残っている。

最初の試行では、永続状態ディレクトリ配下のUnixドメインソケットパスが195文字となり、ワーカー起動前に失敗した。Unixドメインソケットを短いユーザー実行時ディレクトリへ分離してから上記の再試験に成功したため、この変更は実Herdrワーカーで受け入れ条件を成立させるために必要な修正として含めた。

拡張のソースには`code-review`という名前やスキル本文を検出する処理がなく、一般的なサブエージェント委譲の説明と利用指針だけでツールが選択された。一方の失敗が他方の受理済み結果を取り消さないこと、および切り詰め表示を含むツール結果全体がPiの標準上限内に収まることは、登録済みツールの実行インターフェースを使う自動テストで別途確認した。

## 自動検査

実装完了後に次のコマンドを実行し、全自動テスト、型検査、テストのアサーション規約検査、およびビルドが成功した。

```bash
npm run check
npm run build
```
