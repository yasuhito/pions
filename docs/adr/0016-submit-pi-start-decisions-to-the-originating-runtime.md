---
status: accepted
---

# Piの着手判断を作成元Runtimeへ提出する

Piから正式レビューの着手を判断する場合、拡張は同じセッションでそのオペレーションを作成したRuntimeだけを判断提出先とする。再取得専用Runtimeは永続状態を読み取れても、作成元Runtimeが所有する開始待機と配送主体を引き継がないため、判断提出や実行再開には使用しない。これにより、オペレーション識別子の所持を実行権限として扱わず、PiセッションをまたぐRuntime所有権の引き継ぎを導入しない初期契約を維持する。
